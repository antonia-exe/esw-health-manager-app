import React from 'react';
import { View, Text, Image, StyleSheet, TouchableOpacity } from 'react-native';

export default function SelectionSpc() {

    return (
        <View style={styles.container}>
            <text>oiiiii</text>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex:1
    },
});
